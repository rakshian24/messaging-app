type User {
  username: String
  email: String
}

type UserListResponse {
  has_more: Boolean
  users_count: Int
  users: [User]
}

type Query {
  getUsers(limit: Int, offset: Int): UserListResponse
}
